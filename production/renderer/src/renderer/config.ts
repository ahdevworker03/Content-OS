// When true, the renderer loads the approved workspace carousel
// (`production/workspace/carousel.json`) written by Workflow 03.
// When false, it loads the bundled sample data in `src/data/carousel.json`.
export const USE_WORKSPACE_DATA = true;